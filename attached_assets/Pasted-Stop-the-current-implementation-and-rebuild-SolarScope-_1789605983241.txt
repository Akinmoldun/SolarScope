Stop the current implementation and rebuild SolarScope as a genuine 3D interactive simulator. Do not continue polishing the existing flat 2D dashboard.

The current result looks like generic AI-generated UI and does not satisfy the project goal. I need a visually convincing 3D scientific demonstration that immediately shows how solar-panel tracking affects sunlight collection.

Use a real WebGL/3D scene, preferably Three.js or React Three Fiber. Do not fake the 3D effect with gradients, flat cards, or a 2D illustration.

Required 3D scene:

- A real 3D solar-panel field made from multiple panel objects
- Individual photovoltaic modules with visible thickness, frames, cells, and supports
- Three selectable tracking modes:
  - Fixed tilt
  - Single-axis tracking
  - Dual-axis tracking
- Panels must physically rotate in 3D according to the simulated Sun position
- A visible 3D Sun that moves across the sky during the day
- A visible light direction or sunlight-ray visualization showing the angle of incoming sunlight
- A ground plane, horizon, sky, and clear spatial depth
- Realistic lighting, shadows, reflections, and ambient occlusion where practical
- Camera orbit, zoom, and pan controls so the user can inspect the field from different angles
- A reset-camera button and a useful default camera angle
- Time scrubbing and animation controls that visibly show the panels tracking the Sun
- Nearby shading represented by an actual 3D object, such as a tree, building, or obstruction, whose shadow moves across the panels
- Cloud cover represented visually in the 3D scene and incorporated into the power calculation
- Clear visual differences between fixed, single-axis, and dual-axis systems

The scientific model must remain separate from the rendering interface. Keep the numerical calculations in transparent, explainable TypeScript functions. The model should include:

- Sun altitude and azimuth as functions of time
- Incidence angle between sunlight and the panel normal
- Power based on panel area, irradiance, efficiency, cosine losses, cloud cover, and shading
- Daily energy and annual financial value
- Tracker hardware cost and payback period

Use an explainable model such as:

P = N × A × η × I × max(0, cos θ) × C × (1 − S)

where:
- N is the panel count
- A is the area of one panel
- η is module efficiency
- I is solar irradiance
- θ is the angle between the sunlight direction and panel normal
- C is the cloud multiplier
- S is the shading fraction

The interface should be designed around the 3D simulation, not around a collection of dashboard cards. The 3D viewport should be the main focus and occupy most of the screen. Controls and results should support the visualization without covering it.

Keep only the most useful interface elements:

- Tracking-mode selector
- Play, pause, run-a-day, reset, and time slider
- Panel count
- Efficiency
- Cloud cover
- Shading controls
- Electricity price
- Tracker cost
- Current power
- Daily energy
- Annual value
- Payback period
- A concise explanation of the current tracking mode

Make the visual style professional and presentation-ready, but avoid generic SaaS dashboard design, excessive rounded cards, fake gradients, meaningless decorative elements, and AI-generated filler text. Every visual element should help explain the science.

The first screen must immediately communicate:
1. The Sun’s position
2. The orientation of the panels
3. The difference between tracking strategies
4. How alignment changes power output

Add an optional comparison mode where three small 3D fields appear side by side: Fixed, Single-axis, and Dual-axis. Each field should show its panels responding differently to the same Sun position. This comparison is important for the investor presentation.

Maintain and update the supporting documentation so that it accurately describes the real implementation. The class hierarchy should document actual classes, such as:

- SolarPanel
- FixedPanel
- SingleAxisTracker
- DualAxisTracker
- Sun
- SolarField
- ShadingObject

The numerical model may remain in pure functions, but the 3D scene should use clearly organized components/classes that I can explain in my report.

Before finishing:

- Remove the current flat 2D solar-field visualization
- Confirm that the panels genuinely rotate in 3D
- Confirm that the camera can orbit around the scene
- Confirm that shadows and sunlight direction are visible
- Confirm that the Sun animation changes panel orientation
- Confirm that the three tracking modes produce different power curves
- Check desktop and mobile layouts
- Run type checking and production build
- Update `docs/solar-scope-project-guide.md`
- Explain which files contain the 3D scene, simulation model, controls, and documentation

Do not describe the project as complete until the 3D scene is actually implemented and tested in the preview.
